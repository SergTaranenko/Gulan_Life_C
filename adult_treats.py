# -*- coding: utf-8 -*-
"""
adult_treats.py — «Ледник Шамана»
Концепция: каждый вкус мороженого — зашифрованная награда для взрослых.
Расшифровка только через /шаман.
Некоторые вкусы закрыты до определённого ранга.
"""

# ── Таблица вкусов ────────────────────────────────────────────────────────────
# Формат: (id, "Название вкуса", "Расшифровка", min_rank)
# min_rank — с какого ранга (0-11) доступен вкус
# Расшифровка не показывается в основном боте — только через /шаман

ICE_CREAM_FLAVORS = [
    # ── Ранги 1–3 (min_rank 0) ── базовые, нежные
    (1,  "🍦 Пломбир классический",    "смотри тайник", 0),
    (2,  "🍦 Ванильное",               "смотри тайник", 0),
    (3,  "🍦 Сливочное нежное",        "смотри тайник", 0),
    (4,  "🍦 Молочное белое",          "смотри тайник", 0),
    (5,  "🍦 Пломбир в вафельном",     "смотри тайник", 0),
    (6,  "🍦 Сливочно-карамельное",    "смотри тайник", 0),

    # ── Ранги 4–6 (min_rank 3) ── теплее
    (7,  "🍧 Лимонный сорбет",         "смотри тайник", 3),
    (8,  "🍧 Апельсиновое льдяное",    "смотри тайник", 3),
    (9,  "🍧 Малиновый сорбет",        "смотри тайник", 3),
    (10, "🍧 Клубничное",              "смотри тайник", 3),
    (11, "🍧 Вишнёвое",               "смотри тайник", 3),
    (12, "🍧 Гранатовое",              "смотри тайник", 3),

    # ── Ранги 4–6 продолжение ── чуть смелее
    (13, "🍨 Фисташковое",             "смотри тайник", 3),
    (14, "🍨 Фисташково-медовое",      "смотри тайник", 3),
    (15, "🍨 Фундуковое",              "смотри тайник", 3),

    # ── Ранги 6–8 (min_rank 5) ── горячее
    (16, "🍨 Шоколадное тёмное",       "смотри тайник", 5),
    (17, "🍨 Шоколад с перцем",        "смотри тайник", 5),
    (18, "🍨 Горький шоколад",         "смотри тайник", 5),
    (19, "🍨 Молочный шоколад",        "смотри тайник", 5),
    (20, "🍨 Шоколадно-ореховое",      "смотри тайник", 5),
    (21, "🍨 Трюфельное",              "смотри тайник", 5),

    # ── Ранги 8–10 (min_rank 7) ── жарко
    (22, "🍡 Маття (зелёный чай)",     "смотри тайник", 7),
    (23, "🍡 Кокосовое",               "смотри тайник", 7),
    (24, "🍡 Манговое тропическое",    "смотри тайник", 7),
    (25, "🍡 Карамельное солёное",     "смотри тайник", 7),
    (26, "🍡 Лавандовое",              "смотри тайник", 7),
    (27, "🍡 Розовое шампанское",      "смотри тайник", 7),

    # ── Ранги 10–12 (min_rank 9) ── только для завершивших путь
    (28, "🍬 Чёрная смородина",        "смотри тайник", 9),
    (29, "🍬 Черника с базиликом",     "смотри тайник", 9),
    (30, "🍬 Инжирное",                "смотри тайник", 9),
    (31, "🍬 Малина-розмарин",         "смотри тайник", 9),
    (32, "🍬 Эспрессо-ночное",         "смотри тайник", 9),
    (33, "🍬 Красный бархат",          "смотри тайник", 9),

    # ── Только ранг 12 (min_rank 11) ── награды Созидателя
    (34, "👑 Золотое трюфельное",      "смотри тайник", 11),
    (35, "👑 Шафрановое королевское",  "смотри тайник", 11),
    (36, "👑 Платиновый пломбир",      "смотри тайник", 11),
]

# Быстрый словарь по id
FLAVORS_BY_ID = {f[0]: f for f in ICE_CREAM_FLAVORS}

def get_available_flavors(rank_index: int) -> list:
    """Возвращает вкусы, доступные на текущем ранге"""
    return [f for f in ICE_CREAM_FLAVORS if f[3] <= rank_index]

def get_random_flavor(rank_index: int) -> tuple | None:
    """Возвращает случайный доступный вкус"""
    available = get_available_flavors(rank_index)
    return random.choice(available) if available else None

def get_cipher_text(rank_index: int) -> str:
    """Полная расшифровка Ледника Шамана для текущего ранга"""
    available = get_available_flavors(rank_index)
    locked = [f for f in ICE_CREAM_FLAVORS if f[3] > rank_index]

    lines = ["🧊 ЛЕДНИК ШАМАНА — СЕКРЕТНЫЙ РЕЕСТР\n"]
    lines.append("Доступные вкусы:\n")
    for fid, name, meaning, _ in available:
        lines.append(f"{fid}. {name}\n   → {meaning}\n")

    if locked:
        lines.append(f"\n🔒 Закрытых вкусов: {len(locked)}")
        lines.append("Откроются с повышением ранга.")

    return "\n".join(lines)

def get_flavor_display_name(fid: int) -> str:
    """Только название без расшифровки — для основного бота"""
    f = FLAVORS_BY_ID.get(fid)
    return f[1] if f else "🍦 Мороженое"

import random
