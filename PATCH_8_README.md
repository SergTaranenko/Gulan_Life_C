# Патч ранга 8 — «Строитель чужого дома» (категория 1)

Собран на базе **твоей последней версии 2026.09.13** (core_bot.py 1934 строки, с дофамин-пушами v3.1,
микро-наградами и голодом по рангу). Твой `ranks/rank_08.py` сохранён целиком — в конец добавлен блок `PATCH-8`.

## Что положить на сервер
```
/app/
  core_bot.py        ← твоё ядро 2026.09.13 + 22 вставки с меткой PATCH-8 (логика не менялась)
  stones.py          ← НОВЫЙ: камни, слоты, гимн, шмотка, сцены — чистые функции
  img_cache.py       ← НОВЫЙ: кэш картинок GigaChat в /app/data/img/, каждая вторая — из папки
  ranks/rank_08.py   ← твой rank_08 + блок PATCH-8 в конце (step_phrases, slots, hymn_lines, shmotka, scenes)
  core_bot.PATCH-8.diff ← unified diff ядра, если хочешь глянуть глазами или применить через patch
```
Зависимостей новых нет. `path_data.json` получит 5 новых ключей сам — `load_data`
их не трогает, `stones.ensure_keys` добавляет при первом камне. Старые ранги работают как раньше:
все новые поля читаются через `rank.get(...)`.

Если с 13.09 ядро на сервере ещё менялось — примени `core_bot.PATCH-8.diff` или внеси вставки
по списку ниже вручную: каждая помечена `# PATCH-8`.

## Что изменилось в ядре (все места помечены PATCH-8)
1. **Импорты** — `import stones`, `from img_cache import cached_image`.
2. **Клавиатура** — кнопки «🧱 Камень» и «🎵 Гимн».
3. **Сытость и голод** — «пытался» +2 ч, камень +4 ч, дело +12 ч (в ядре, метки PATCH-8).
   Пороги голода в rank_08: предупреждение 6 ч, бунт 10 ч (было 19/34). Голод — сверхсобытие: сработает только
   если реально выпал из темпа.
4. **`/step`** (новая) — камень: сытость +4 ч,
   счётчики `stones_total / stones_rank / stones_today / stones_in_deed / stones_slots[утро|день|вечер]`,
   ответ: фраза ранга + `Кладка ▮▮▮▯▯ 3/5` + стена ранга кирпичами. На 5-м камне подсказка нажать «Сделано».
5. **`/hymn`** (новая) — «включи мотивацию на телефоне», 5 вариантов текста в rank_08.
6. **`/done`** — обнуляет камни кладки; после продукта присылает «🎁 купи себе штучку».
7. **Сброс дня** — обнуление `stones_today` и флагов сцены/строки.
8. **13:00** — зарисовка эпохи: случайная сцена ранга (текст + картинка) из пула 16 сцен.
9. **23:00** — строка камней ранга: `🌅 утро N · ☀️ день N · 🌙 вечер N · всего · сегодня · стена`.
   Живёт весь ранг, обнуляется при переходе (`check_date_transitions`).
10. **Кнопки и свободный текст** — «камень/шаг» → `/step`, «гимн/песн/стрел» → `/hymn`; регистрация хендлеров.
11. **Картинки** — все 7 вызовов `gigachat.generate_image(...)` → `cached_image(gigachat, ...)`.
    Рассветная и ночная картинки берут промпт из пула сцен ранга, если он есть.

Слоты: утро — до 10:00, день — 10:00–17:00, вечер — с 17:00 (`RANK["slots"]`, можно менять в rank_08).

## rank_08.py — что добавлено в конец твоего файла
15 `step_phrases` (фразы на камень с обращениями), `slots` (10 / 17), 5 `hymn_lines`, 6 `shmotka`,
16 `scenes` (текст зарисовки + промпт). Всё остальное — твоё, как было.

## Проверено
Мок-тест на твоём реальном `path_data.json` (ранг 7 = Строитель, 1 дело, 86 всего): 5×`/step` → подсказка
на 5-м → `/done` закрыл кладку (дел 2, всего 87), продукт, шмотка → `/hymn` → текст «шаг сделан» → `/step`,
«сделал» → `/done`. Микро-награды, дофамин-пуши, adult_treats — не задеты. Компиляция всех файлов — ок.

## Что не трогали (сознательно)
Ранги, дела, дофамин по часам, хранитель, недельный отчёт, шаман, награды, коллекция —
работают как раньше. Камни — отдельный счётчик, к делам не привязаны: `/done` нажимаешь сам.

## Сохранится ли прогресс
Да, если делать так:
- Весь прогресс — ранг, дела за ранг, `total_deeds` (86), долг `penalty_pool`, сверхдела, серии, коллекция,
  показанные переходы — лежит в одном файле `/app/data/path_data.json`. Код его не создаёт заново,
  а читает и дописывает пять новых ключей с нулями (`stones_*`). Ни один старый ключ не удаляется и не обнуляется.
- `/app/data` в Dockerfile объявлена как volume. Если volume на хостинге подключён — файл переживает любой
  редеплой и замену кода, ничего делать не надо.
- ВНИМАНИЕ: в архиве 2026.09.13 есть `data/path_data.json`, и Dockerfile делает `COPY . .` — то есть этот файл
  попадает в образ. Если volume подключён, он перекроет файл из образа (volume всегда главнее) — всё ок.
  Если volume НЕ подключён, при каждом редеплое бот стартует с тем `path_data.json`, который лежал в папке проекта
  на момент сборки. Поэтому перед деплоем: скачай с сервера актуальный `path_data.json` и положи его в `data/`
  в проекте — тогда даже без volume ничего не потеряется.
- Проверка после деплоя: `/status` должен показать ранг «Строитель чужого дома», дел за ранг столько же, сколько было,
  путь 86+/174. Если увидел «Ранг 1» или 0/174 — значит подхватился пустой файл: верни `path_data.json` в `/app/data`
  и перезапусти контейнер, ничего не потеряно, пока есть копия.
